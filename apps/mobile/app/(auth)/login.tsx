import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function LoginScreen(): React.ReactElement {
  return <ScreenPlaceholder title="Sign in" subtitle="Authentication arrives in a later phase." />;
}
