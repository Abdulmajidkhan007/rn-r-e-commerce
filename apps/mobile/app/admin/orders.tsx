import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function AdminOrdersScreen(): React.ReactElement {
  return <ScreenPlaceholder title="Admin · Orders" subtitle="Manage customer orders." />;
}
