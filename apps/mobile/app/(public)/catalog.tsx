import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function CatalogScreen(): React.ReactElement {
  return <ScreenPlaceholder title="Catalog" subtitle="Browse kids' clothing by category." />;
}
