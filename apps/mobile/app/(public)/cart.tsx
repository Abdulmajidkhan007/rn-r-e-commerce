import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function CartScreen(): React.ReactElement {
  return <ScreenPlaceholder title="Cart" subtitle="Your shopping cart." />;
}
