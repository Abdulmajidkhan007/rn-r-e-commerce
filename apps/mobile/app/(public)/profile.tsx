import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function ProfileScreen(): React.ReactElement {
  return <ScreenPlaceholder title="Profile" subtitle="Account and settings." />;
}
