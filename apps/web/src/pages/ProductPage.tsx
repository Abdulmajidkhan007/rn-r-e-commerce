import { PagePlaceholder } from './PagePlaceholder';

export default function ProductPage(): React.ReactElement {
  return (
    <PagePlaceholder title="Product" subtitle="Product detail — wired to a real product later." />
  );
}
