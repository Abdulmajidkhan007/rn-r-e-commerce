import { PagePlaceholder } from './PagePlaceholder';

export default function NotFoundPage(): React.ReactElement {
  return <PagePlaceholder title="404 — Not found" subtitle="This page does not exist." />;
}
