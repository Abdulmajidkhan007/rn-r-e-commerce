import { PagePlaceholder } from './PagePlaceholder';

export default function CheckoutPage(): React.ReactElement {
  return <PagePlaceholder title="Checkout" subtitle="Checkout with deposit payment — later." />;
}
