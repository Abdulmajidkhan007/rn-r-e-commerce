import { PagePlaceholder } from './PagePlaceholder';

export default function AdminOrdersPage(): React.ReactElement {
  return <PagePlaceholder title="Admin · Orders" subtitle="Manage customer orders." />;
}
