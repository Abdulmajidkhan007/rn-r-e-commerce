import { PagePlaceholder } from './PagePlaceholder';

export default function RegisterPage(): React.ReactElement {
  return <PagePlaceholder title="Sign up" subtitle="Authentication arrives in a later phase." />;
}
