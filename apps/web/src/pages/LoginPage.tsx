import { PagePlaceholder } from './PagePlaceholder';

export default function LoginPage(): React.ReactElement {
  return <PagePlaceholder title="Sign in" subtitle="Authentication arrives in a later phase." />;
}
