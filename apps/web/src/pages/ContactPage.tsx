import { PagePlaceholder } from './PagePlaceholder';

export default function ContactPage(): React.ReactElement {
  return <PagePlaceholder title="Contact" subtitle="Get in touch with KidsWear." />;
}
