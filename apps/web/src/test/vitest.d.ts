// Registers @testing-library/jest-dom's matchers with vitest's Assertion type.
// The setup file imports this at runtime; tsc needs it declared project-wide.
import '@testing-library/jest-dom/vitest';
