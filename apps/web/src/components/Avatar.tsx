import MuiAvatar, { type AvatarProps } from '@mui/material/Avatar';

/** App avatar — MUI Avatar. */
export function Avatar(props: AvatarProps): React.ReactElement {
  return <MuiAvatar {...props} />;
}
